# Core
requests>=2.31
pandas>=2.1
pyarrow>=14.0
pyyaml>=6.0

# Data sources
biopython>=1.83          # NCBI E-utilities
ucimlrepo>=0.0.7         # UCI dataset

# Optional convenience (requires Python >=3.10):
# pgscatalog-utils>=0.5
# We compute PRS ourselves with a weighted sum on day 2; this package is not
# required. Install separately if you have Python 3.10+ and want its CLI.

# Modeling (used in day 2 onward)
scikit-learn>=1.4
xgboost>=2.0
imbalanced-learn>=0.12
joblib>=1.3
shap>=0.45
tabulate>=0.9                # comparison.md uses pandas.to_markdown()

# Tracking
mlflow>=2.10

# API (used in day 3 onward)
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
httpx>=0.27                  # for TestClient (FastAPI integration tests)
requests>=2.31

# Frontend (used in the day-5+ UI)
streamlit>=1.30
streamlit-autorefresh>=1.0

# Drift (used in days 5-6)
evidently>=0.4

# Development
pytest>=8.0
ruff>=0.3
